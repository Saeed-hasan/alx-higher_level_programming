#!/usr/bin/python3
def multiple_returns(sentence):
    s-length = len(sentence)
    if s-length == 0:
        first character = None
    else:
        first character = sentence[0]
    return ((s-length, first character))
